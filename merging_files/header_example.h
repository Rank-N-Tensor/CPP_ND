#ifndef HEADER_EXAMPLE_H  // these are include guards , this line means : if header_example is not defined,
#define HEADER_EXAMPLE_H // then, define header_example

void OuterFunction(int);
void InnerFunction(int);

#endif